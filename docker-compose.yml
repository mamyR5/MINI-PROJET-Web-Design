version: '3.8'

services:
  db:
    image: postgres:15
    container_name: db_web_design
    environment:
      POSTGRES_USER: user_admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: web_design_db
    ports:
      - "5433:5432"

  tomcat:
    build: .
    container_name: tomcat_app
    ports:
      - "8081:8080"
    depends_on:
      - db